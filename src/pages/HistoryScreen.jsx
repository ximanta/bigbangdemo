import React from 'react';import {useNavigate} from 'react-router-dom';import Button from '../components/Button.jsx';import {mockHistory} from '../data/mockData.js';const HistoryScreen = () => {  const navigate = useNavigate();  const handleRetakeAssessment = () => {    navigate('/questionnaire');  };  return (    <div className="page-container">      <h1 className="page-header">Your Health Journey</h1>      <p className="page-description">Track your Biological Age over time and see your progress.</p>      <div className="history-chart-placeholder">        {mockHistory.length > 0 ? (          <span>Biological Age Trend (Simulated Chart)</span>        ) : (          <span>No past assessments yet.</span>        )}      </div>      <h2 className="page-header" style={{fontSize: '1.8em', marginTop: '30px'}}>Past Assessments</h2>      {mockHistory.length > 0 ? (        <div className="assessment-history-list">          {mockHistory.map((entry, index) => (            <div key={index} className="assessment-history-item">              <span className="date">{entry.date}</span>              <span className="age-score">Biological Age: {entry.biologicalAge}</span>            </div>          ))}        </div>      ) : (        <p className="page-description">Take your first assessment to see your history here!</p>      )}      <Button onClick={handleRetakeAssessment} className="button" style={{marginTop: '30px'}}>        Retake Assessment      </Button>    </div>  );};export default HistoryScreen;