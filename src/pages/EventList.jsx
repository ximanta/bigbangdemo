import React from 'react';import { useEvents } from '../context/EventContext';import { EventCard } from '../components/EventCard';import { LoadingSpinner } from '../components/LoadingSpinner';export const EventList = () => {  const { events } = useEvents();  if (!events) {    return <LoadingSpinner center />;  }  return (    <div className="container">      <h2>Upcoming Events</h2>      {events.length === 0 ? (        <p className="text-center">No events available at the moment. Check back soon!</p>      ) : (        <div className="card-grid">          {events.map((event) => (            <EventCard key={event.id} event={event} />          ))}        </div>      )}    </div>  );};