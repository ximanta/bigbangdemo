import React, { useState } from 'react';import InputField from '../../components/InputField';import Button from '../../components/Button';import Card from '../../components/Card';import Notification from '../../components/Notification';import { Mail } from 'lucide-react';const SubscriptionPage = () => {  const [email, setEmail] = useState('');  const [notification, setNotification] = useState(null);  const handleSubscribe = (e) => {    e.preventDefault();    if (email) {      // Mock subscription logic      console.log(`Subscribing ${email}`);      setNotification({ message: `Thank you for subscribing, ${email}! Check your inbox.`, type: 'success' });      setEmail('');    } else {      setNotification({ message: 'Please enter a valid email address.', type: 'error' });    }  };  return (    <div className="flex-container items-center justify-center min-h-screen bg-gray-100">      <Card title="Subscribe to Our Newsletter" className="w-full" style={{ maxWidth: '450px' }}>        <p className="text-muted text-center mb-4">          Get personalized news delivered directly to your inbox.        </p>        <form onSubmit={handleSubscribe}>          <InputField            label="Email Address"            id="subscribeEmail"            type="email"            value={email}            onChange={(e) => setEmail(e.target.value)}            placeholder="your@example.com"            required          />          <Button            type="submit"            variant="primary"            className="w-full mt-4"          >            <Mail size={18} className="mr-2" /> Subscribe Now          </Button>        </form>        <p className="text-center text-sm text-muted mt-4">          By subscribing, you agree to our <a href="#" className="text-primary">Privacy Policy</a>.        </p>      </Card>      {notification && (        <Notification          message={notification.message}          type={notification.type}          onClose={() => setNotification(null)}        />      )}    </div>  );};export default SubscriptionPage;