import React, {useState} from 'react';import Button from '../components/Button.jsx';import {recommendations} from '../data/mockData.js';const RecommendationsScreen = () => {  const [activeCategory, setActiveCategory] = useState('All');  const categories = ['All', ...new Set(recommendations.map((rec) => rec.category))];  const filteredRecommendations = activeCategory === 'All'    ? recommendations    : recommendations.filter((rec) => rec.category === activeCategory);  return (    <div className="page-container recommendations-container">      <h1 className="page-header">Personalized Recommendations</h1>      <p className="page-description">      Based on your assessment, here are some actionable tips to improve your health.      </p>      <div className="category-filters">        {categories.map((category) => (          <Button            key={category}            onClick={() => setActiveCategory(category)}            className={activeCategory === category ? 'active' : ''}            variant="outline"            size="small"          >            {category}          </Button>        ))}      </div>      <div className="recommendation-list">        {filteredRecommendations.length > 0 ? (          filteredRecommendations.map((rec) => (            <div key={rec.id} className="recommendation-card">              <h3>{rec.title}</h3>              <p>{rec.description}</p>            </div>          ))        ) : (          <p className="page-description">No recommendations found for this category.</p>        )}      </div>    </div>  );};export default RecommendationsScreen;