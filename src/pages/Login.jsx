import React, { useState } from 'react';import { Button } from '../components/Button';import { Input } from '../components/Input';import { Card } from '../components/Card';export const Login = ({ onLogin }) => { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const handleSubmit = (e) => { e.preventDefault(); if (email === 'admin@cloudkitchen.com' && password === 'password') { onLogin(true); } else { setError('Invalid email or password.'); } }; return ( <div className="auth-container"> <Card className="auth-card"> <h2>Cloud Kitchen Manager</h2> {error && <p className="alert-error text-center">{error}</p>} <form onSubmit={handleSubmit}> <Input label="Email" id="email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="admin@cloudkitchen.com" required={true} /> <Input label="Password" id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="password" required={true} /> <Button type="submit" className="button-primary" style={{ width:'100%', marginTop:'20px' }}> Log In </Button> </form> </Card> </div> );};