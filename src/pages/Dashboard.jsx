import React from 'react';import { Card } from '../components/Card';import { ChartPlaceholder } from '../components/ChartPlaceholder';import { Badge } from '../components/Badge';import { mockOrders, mockInventory } from '../data/mockData';export const Dashboard = () => { const ordersInProgress = mockOrders.filter( order => ['New', 'Preparing', 'Ready', 'Dispatched'].includes(order.status) ).length; const lowStockItems = mockInventory.filter( item => item.currentStock <= item.reorderPoint ).length; const totalSalesToday = mockOrders .filter(order => new Date(order.orderTime).toDateString() === new Date().toDateString()) .reduce((sum, order) => sum + order.total, 0) .toFixed(2); return ( <div className="main-content"> <h1>Dashboard</h1> <div className="card-grid mb-20"> <Card title="Orders In Progress"> <p style={{ fontSize:'2em', fontWeight:'bold' }}>{ordersInProgress}</p> <p>Currently being processed</p> </Card> <Card title="Low Stock Alerts"> <p style={{ fontSize:'2em', fontWeight:'bold', color:lowStockItems > 0 ? '#e74c3c' : '#2ecc71' }}> {lowStockItems} </p> <p>Items below reorder point</p> </Card> <Card title="Total Sales Today"> <p style={{ fontSize:'2em', fontWeight:'bold' }}>${totalSalesToday}</p> <p>Revenue generated so far</p> </Card> </div> <Card title="Recent Orders"> <div className="table-container"> <table className="data-table"> <thead> <tr> <th>Order ID</th> <th>Customer</th> <th>Partner</th> <th>Status</th> <th>Total</th> </tr> </thead> <tbody> {mockOrders.slice(0, 5).map(order => ( <tr key={order.id}> <td>{order.id}</td> <td>{order.customerName}</td> <td>{order.deliveryPartner}</td> <td> <Badge type={ order.status === 'New' ? 'new' : order.status === 'Preparing' ? 'preparing' : order.status === 'Ready' ? 'ready' : order.status === 'Dispatched' ? 'dispatched' : 'completed' } > {order.status} </Badge> </td> <td>${order.total.toFixed(2)}</td> </tr> ))} </tbody> </table> </div> </Card> <div className="card-grid mt-20"> <Card title="Sales Trends"> <ChartPlaceholder title="Monthly Sales Trend" /> </Card> <Card title="Popular Menu Items"> <ChartPlaceholder title="Top 5 Menu Items" /> </Card> </div> </div> );};