import React from 'react';import {Link} from 'react-router-dom';import Button from '../components/Button.jsx';import {mockUser, mockHistory} from '../data/mockData.js';const HomeScreen = () => {  const latestAssessment = mockHistory.length > 0    ? mockHistory[mockHistory.length - 1]    : null;  const ageDifference = latestAssessment    ? mockUser.actualAge - latestAssessment.biologicalAge    : 0;  const comparisonText = latestAssessment    ? ageDifference > 0      ? `You are ${Math.abs(ageDifference)} years biologically younger!`      : ageDifference < 0        ? `You are ${Math.abs(ageDifference)} years biologically older.`        : 'Your biological age matches your actual age!'    : 'Take your first assessment!';  return (    <div className="page-container">      <div className="home-hero">        <h1>Hello, {mockUser.name}!</h1>        <p>Welcome back to your personalized health journey.</p>      </div>      <div className="home-card">        <h2>Your Latest Health Snapshot</h2>        {latestAssessment ? (          <>            <p>Your Actual Age: <span className="current-age-display">{mockUser.actualAge}</span></p>            <p>Your Biological Age: <span className="current-age-display" style={{color: 'var(--primary-dark)'}}>{latestAssessment.biologicalAge}</span></p>            <p className="small-text">{comparisonText}</p>            <Button to="/results" className="button small" variant="secondary">              View Details            </Button>          </>        ) : (          <>            <p className="no-results">No assessment results yet. Let's find out your Biological Age!</p>            <Button to="/questionnaire" className="button">              Start Assessment            </Button>          </>        )}      </div>      <div className="home-card">        <h2>Quick Actions</h2>        <Button to="/questionnaire" className="button outline" style={{marginRight: '10px'}}>          <span style={{marginRight: '8px'}}>Start New Assessment</span>        </Button>        <Button to="/recommendations" className="button outline">          <span style={{marginRight: '8px'}}>Explore Recommendations</span>        </Button>      </div>    </div>  );};export default HomeScreen;