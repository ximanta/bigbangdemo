import React, { useState } from 'react';import { recommendations } from '../utils/mockData.js';import Button from '../components/Button.jsx';function Recommendations() {const [activeCategory, setActiveCategory] = useState('All');const allCategories = ['All', ...new Set(recommendations.map((rec) => rec.category))];const filteredRecommendations =activeCategory === 'All'? recommendations: recommendations.filter((rec) => rec.category === activeCategory);return (<div className="container recommendations-page"><h2 className="text-center card-title mb-20">Personalized Recommendations</h2><div className="recommendation-filters"><Buttonvariant="secondary"size="small"className={`recommendation-filter-button ${activeCategory === 'All' ? 'active' : ''}`}onClick={() => setActiveCategory('All')}>All</Button>{allCategories.filter(cat => cat !== 'All').map((category) => (<Buttonkey={category}variant="secondary"size="small"className={`recommendation-filter-button ${activeCategory === category ? 'active' : ''}`}onClick={() => setActiveCategory(category)}>{category.replace(/_/g, ' ').split(' ').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ')}</Button>))}<div/></div><div className="recommendation-list">{filteredRecommendations.length === 0 ? (<p className="text-center text-light-color">No recommendations found for this category.</p>) : (filteredRecommendations.map((rec) => (<div key={rec.id} className="recommendation-card"><h3 className="recommendation-card-title">{rec.title}</h3><p className="recommendation-card-description">{rec.description}</p></div>)))}</div></div>);}.recommendations-page {padding-bottom: 80px;}.recommendation-filters {display: flex;flex-wrap: wrap;gap: 10px;margin-bottom: 20px;justify-content: center;}.recommendation-filter-button {padding: 8px 15px;border-radius: 20px;background-color: var(--background-color);border: 1px solid var(--border-color);color: var(--text-light-color);cursor: pointer;transition: background-color 0.2s, color 0.2s, border-color 0.2s;}.recommendation-filter-button.active {background-color: var(--primary-color);color: var(--surface-color);border-color: var(--primary-color);}.recommendation-filter-button:hover:not(.active) {background-color: var(--primary-color);color: var(--surface-color);border-color: var(--primary-color);opacity: 0.8;}export default Recommendations;