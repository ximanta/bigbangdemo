import React, {useState} from 'react';import {Link} from 'react-router-dom';import InputField from '../components/InputField.jsx';import Button from '../components/Button.jsx';import {Google, Apple} from 'lucide-react';const SignUpScreen = ({onSignUp}) => {  const [email, setEmail] = useState('');  const [password, setPassword] = useState('');  const [confirmPassword, setConfirmPassword] = useState('');  const handleSubmit = (e) => {    e.preventDefault();    if (password !== confirmPassword) {      alert('Passwords do not match!');      return;    }    console.log('Sign Up attempt with:', {email, password});    onSignUp();  };  return (    <div className="page-container">      <h1 className="page-header">Create Your Account</h1>      <p className="page-description">Join us to discover your Biological Age.</p>      <form onSubmit={handleSubmit} style={{width: '100%', maxWidth: '400px'}}>        <InputField          label="Email"          id="email"          type="email"          value={email}          onChange={(e) => setEmail(e.target.value)}          placeholder="your@example.com"          required        />        <InputField          label="Password"          id="password"          type="password"          value={password}          onChange={(e) => setPassword(e.target.value)}          placeholder="••••••••"          required        />        <InputField          label="Confirm Password"          id="confirmPassword"          type="password"          value={confirmPassword}          onChange={(e) => setConfirmPassword(e.target.value)}          placeholder="••••••••"          required        />        <Button type="submit">          Sign Up        </Button>      </form>      <div style={{marginTop: '20px', color: 'var(--text-light)'}}>        — Or sign up with —      </div>      <div className="social-login-buttons">        <Button className="button" onClick={() => console.log('Google signup')}>          <Google size={18} />          Google        </Button>        <Button className="button" onClick={() => console.log('Apple signup')}>          <Apple size={18} />          Apple        </Button>      </div>      <p style={{marginTop: '30px'}}>        Already have an account?{' '}        <Link to="/login" className="form-link">          Login        </Link>      </p>    </div>  );};export default SignUpScreen;