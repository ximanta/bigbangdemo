import React, {useState} from 'react';import InputField from '../components/InputField.jsx';import Button from '../components/Button.jsx';import {mockUser} from '../data/mockData.js';const ProfileSetupScreen = ({onSave}) => {  const [name, setName] = useState(mockUser.name || '');  const [dob, setDob] = useState('1988-01-01'); // Mock DOB  const [gender, setGender] = useState(mockUser.gender || 'Female');  const [height, setHeight] = useState(mockUser.height || '');  const [weight, setWeight] = useState(mockUser.weight || '');  const [privacyConsent, setPrivacyConsent] = useState(false);  const handleSubmit = (e) => {    e.preventDefault();    if (!privacyConsent) {      alert('Please agree to the privacy consent.');      return;    }    console.log('Profile saved:', {name, dob, gender, height, weight, privacyConsent});    onSave();  };  return (    <div className="page-container">      <h1 className="page-header">Set Up Your Profile</h1>      <p className="page-description">Tell us a little about yourself to get started.</p>      <div className="profile-avatar-placeholder">        <span style={{fontSize: '1em'}}>Avatar</span>      </div>      <form onSubmit={handleSubmit} style={{width: '100%', maxWidth: '400px'}}>        <InputField          label="Full Name"          id="name"          type="text"          value={name}          onChange={(e) => setName(e.target.value)}          placeholder="John Doe"          required        />        <InputField          label="Date of Birth"          id="dob"          type="date"          value={dob}          onChange={(e) => setDob(e.target.value)}          required        />        <div className="input-field-container">          <label htmlFor="gender">Gender</label>          <select id="gender" value={gender} onChange={(e) => setGender(e.target.value)} required>            <option value="">Select...</option>            <option value="Male">Male</option>            <option value="Female">Female</option>            <option value="Other">Other</option>          </select>        </div>        <InputField          label="Height (cm)"          id="height"          type="number"          value={height}          onChange={(e) => setHeight(e.target.value)}          placeholder="e.g., 175"          required          min="50"          max="250"        />        <InputField          label="Weight (kg)"          id="weight"          type="number"          value={weight}          onChange={(e) => setWeight(e.target.value)}          placeholder="e.g., 70"          required          min="20"          max="300"        />        <div className="checkbox-container">          <input            type="checkbox"            id="privacyConsent"            checked={privacyConsent}            onChange={(e) => setPrivacyConsent(e.target.checked)}            required          />          <label htmlFor="privacyConsent">            I agree to the <Link to="/privacy-policy" className="form-link" style={{display: 'inline'}}>Privacy Policy</Link>          </label>        </div>        <Button type="submit">          Save Profile        </Button>      </form>    </div>  );};export default ProfileSetupScreen;