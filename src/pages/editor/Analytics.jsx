import React from 'react';import Card from '../../components/Card';import { mockNewsletters } from '../../data/mockData';import Table from '../../components/Table';import { formatDateTime } from '../../utils/helpers';const Analytics = () => {  const newsletterPerformanceHeaders = ['Title', 'Scheduled Date', 'Status', 'Delivery Rate', 'Open Rate'];  const renderNewsletterRow = (newsletter) => (    <>      <td>{newsletter.title}</td>      <td>{formatDateTime(newsletter.scheduledDate)}</td>      <td>        <span          className={`badge ${            newsletter.status === 'Sent' ? 'badge-success' : 'badge-info'          }`}        >          {newsletter.status}        </span>      </td>      <td>{newsletter.deliveryRate}</td>      <td>{newsletter.openRate}</td>    </>  );  return (    <div>      <h2 className="text-2xl font-semibold mb-6">Analytics Overview</h2>      <div className="grid-container grid-col-2 mb-6">        <Card title="Subscriber Growth">          <div className="chart-placeholder">            <span>Subscriber Growth Chart</span>          </div>        </Card>        <Card title="Open Rates Over Time">          <div className="chart-placeholder">            <span>Open Rate Trends Chart</span>          </div>        </Card>      </div>      <Card title="Newsletter Performance">        <Table          headers={newsletterPerformanceHeaders}          data={mockNewsletters}          renderRow={renderNewsletterRow}          keyExtractor={(item) => item.id}        />      </Card>    </div>  );};export default Analytics;