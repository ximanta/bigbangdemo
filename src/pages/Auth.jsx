import React, { useState } from 'react';import { useNavigate, Link } from 'react-router-dom';import InputField from '../components/InputField.jsx';import Button from '../components/Button.jsx';import { Google, Apple } from 'lucide-react'; // Example icons, adjust as neededfunction Auth({ onLogin, showToast }) {const [isLogin, setIsLogin] = useState(true);const [email, setEmail] = useState('');const [password, setPassword] = useState('');const navigate = useNavigate();const handleSubmit = (e) => {e.preventDefault();if (!email || !password) {showToast('Please enter both email and password.', 'error');return;}// For demo, just simulate login/signup successif (isLogin) {if (email === 'test@example.com' && password === 'password') {const userProfile = JSON.parse(localStorage.getItem('userProfile')) || { email: 'test@example.com', name: 'Demo User' }; // Retrieve or create dummy profileonLogin(userProfile);} else {showToast('Invalid credentials. Try test@example.com / password', 'error');}} else { // Signup logic for demo: create a minimal profile and log inconst newProfile = {email,name: 'New User',dateOfBirth: '',gender: '',height: '',weight: '',privacyConsent: true,};onLogin(newProfile);showToast('Account created and logged in!', 'success');navigate('/profile-setup', { state: { newUser: true } });}};return (<div className="auth-container"><h2 className="text-center card-title">{isLogin ? 'Login' : 'Sign Up'}</h2><form onSubmit={handleSubmit} className="mt-20"><InputFieldlabel="Email"type="email"name="email"value={email}onChange={(e) => setEmail(e.target.value)}placeholder="your@example.com"required/><InputFieldlabel="Password"type="password"name="password"value={password}onChange={(e) => setPassword(e.target.value)}placeholder="••••••••"required/><Buttontype="submit"fullWidth>{isLogin ? 'Login' : 'Sign Up'}</Button></form>{isLogin && (<div className="text-center mt-20"><Link to="#" className="button-ghost">Forgot Password?</Link></div>)}<div className="text-center auth-switch"><p>{isLogin ? "Don't have an account?" : "Already have an account?"}{' '}<button onClick={() => setIsLogin(!isLogin)} className="button-ghost">{isLogin ? 'Sign Up' : 'Login'}</button></p></div><div className="social-login-buttons"><Button variant="secondary" fullWidth><Google size={20} /> Login with Google</Button><Button variant="secondary" fullWidth><Apple size={20} /> Login with Apple</Button></div></div>);}.auth-container {max-width: 400px;margin: 50px auto;padding: 30px;background-color: var(--surface-color);border-radius: 12px;box-shadow: 0 4px 12px var(--shadow-color);}.auth-switch {margin-top: 25px;font-size: 0.9em;}.auth-switch a {color: var(--primary-color);text-decoration: none;font-weight: bold;}.auth-switch a:hover {text-decoration: underline;}.social-login-buttons {display: flex;flex-direction: column;gap: 15px;margin-top: 25px;}.social-login-button {display: flex;align-items: center;justify-content: center;gap: 10px;padding: 12px 20px;border: 1px solid var(--border-color);border-radius: 8px;background-color: var(--surface-color);color: var(--text-color);font-size: 1em;cursor: pointer;transition: background-color 0.2s ease-in-out;}.social-login-button svg {width: 20px;height: 20px;}.social-login-button:hover {background-color: var(--background-color);}export default Auth;