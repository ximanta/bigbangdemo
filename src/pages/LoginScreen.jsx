import React, {useState} from 'react';import {Link} from 'react-router-dom';import InputField from '../components/InputField.jsx';import Button from '../components/Button.jsx';import {Mail, Lock, Google, Apple} from 'lucide-react';const LoginScreen = ({onLogin}) => {  const [email, setEmail] = useState('');  const [password, setPassword] = useState('');  const handleSubmit = (e) => {    e.preventDefault();    console.log('Login attempt with:', {email, password});    onLogin();  };  return (    <div className="page-container">      <h1 className="page-header">Welcome Back</h1>      <p className="page-description">Log in to continue your health journey.</p>      <form onSubmit={handleSubmit} style={{width: '100%', maxWidth: '400px'}}>        <InputField          label="Email"          id="email"          type="email"          value={email}          onChange={(e) => setEmail(e.target.value)}          placeholder="your@example.com"          required        />        <InputField          label="Password"          id="password"          type="password"          value={password}          onChange={(e) => setPassword(e.target.value)}          placeholder="••••••••"          required        />        <Link to="/forgot-password" className="form-link" style={{marginBottom: '20px'}}>          Forgot Password?        </Link>        <Button type="submit">          Login        </Button>      </form>      <div style={{marginTop: '20px', color: 'var(--text-light)'}}>        — Or continue with —      </div>      <div className="social-login-buttons">        <Button className="button" onClick={() => console.log('Google login')}>          <Google size={18} />          Google        </Button>        <Button className="button" onClick={() => console.log('Apple login')}>          <Apple size={18} />          Apple        </Button>      </div>      <p style={{marginTop: '30px'}}>        Don't have an account?{' '}        <Link to="/signup" className="form-link">          Sign Up        </Link>      </p>    </div>  );};export default LoginScreen;