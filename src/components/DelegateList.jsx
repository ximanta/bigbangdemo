import React from 'react';export const DelegateList = ({ delegates }) => {  if (!delegates || delegates.length === 0) {    return <p>No delegates registered for this event yet.</p>;  }  return (    <div className="card">      <table className="delegate-table">        <thead>          <tr>            <th>Name</th>            <th>Email</th>            <th>Registration Date</th>          </tr>        </thead>        <tbody>          {delegates.map((delegate) => (            <tr key={delegate.id}>              <td>{delegate.name}</td>              <td>{delegate.email}</td>              <td>{delegate.registrationDate}</td>            </tr>          ))}        </tbody>      </table>    </div>  );};