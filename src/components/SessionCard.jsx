import React, { useState } from 'react';import { Clock, User, ChevronDown, ChevronRight } from 'lucide-react';import { Button } from './Button';export const SessionCard = ({ session, onEdit, onDelete, isOrganizerView }) => {  const [isOpen, setIsOpen] = useState(false);  return (    <div className="accordion-item">      <div className="accordion-header" onClick={() => setIsOpen(!isOpen)}>        <div className="accordion-title">          <h4>{session.title}</h4>          <p>            <Clock size={16} style={{ verticalAlign: 'middle', marginRight: '4px' }} />            {session.startTime} - {session.endTime}          </p>          {session.speaker && (            <p>              <User size={16} style={{ verticalAlign: 'middle', marginRight: '4px' }} />              {session.speaker}            </p>          )}        </div>        {isOpen ? <ChevronDown size={24} /> : <ChevronRight size={24} />}      </div>      <div className={`accordion-content ${isOpen ? '' : 'hidden'}`}>        <p>{session.description}</p>        {isOrganizerView && (          <div className="list-item-actions" style={{ marginTop: 'var(--spacing-md)' }}>            <Button onClick={(e) => { e.stopPropagation(); onEdit(session.id); }} variant="secondary">              Edit            </Button>            <Button onClick={(e) => { e.stopPropagation(); onDelete(session.id); }} variant="danger">              Delete            </Button>          </div>        )}      </div>    </div>  );};