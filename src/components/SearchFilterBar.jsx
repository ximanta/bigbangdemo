import React from 'react'; import { Search } from 'lucide-react'; import { TextInput } from './TextInput'; import { Dropdown } from './Dropdown'; import { Button } from './Button'; export const SearchFilterBar = ({ searchTerm, onSearchChange, filters, onFilterChange, onApplyFilters, showApplyButton = false }) => { return ( <div className="search-filter-bar"> <div className="form-group search-input"> <Search size={20} color="#666" /> <TextInput id="search" placeholder="Search by subject or ID..." value={searchTerm} onChange={(e) => onSearchChange(e.target.value)} /> </div> {Object.entries(filters).map(([key, filter]) => ( <Dropdown key={key} id={key} label={filter.label} value={filter.value} onChange={(e) => onFilterChange(key, e.target.value)} options={filter.options} className="filter-dropdown" /> ))} {showApplyButton && ( <Button onClick={onApplyFilters} variant="primary"> Apply Filters </Button> )} </div> ); };