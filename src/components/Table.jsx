import React from 'react';const Table = ({ headers, data, renderRow, keyExtractor }) => {  return (    <div className="table-container">      <table className="table">        <thead>          <tr>            {headers.map((header, index) => (              <th key={index}>{header}</th>            ))}          </tr>        </thead>        <tbody>          {data.length === 0 ? (            <tr>              <td colSpan={headers.length} className="text-center text-muted py-4">                No data available.              </td>            </tr>          ) : (            data.map((item, index) => (              <tr key={keyExtractor ? keyExtractor(item) : index}>                {renderRow(item)}              </tr>            ))          )}        </tbody>      </table>    </div>  );};export default Table;