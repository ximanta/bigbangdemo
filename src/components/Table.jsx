import React from 'react';export const Table = ({ headers, data, renderRow, className = '' }) => { return ( <div className="table-container"> <table className={`data-table ${className}`}> <thead> <tr> {headers.map((header, index) => ( <th key={index}>{header}</th> ))} </tr> </thead> <tbody> {data.length > 0 ? ( data.map((item, index) => renderRow(item, index)) ) : ( <tr> <td colSpan={headers.length} className="text-center"> No data available. </td> </tr> )} </tbody> </table> </div> );};