import React, { useEffect, useState } from 'react';import { CheckCircle, XCircle, Info } from 'lucide-react';const Notification = ({ message, type = 'info', duration = 3000, onClose }) => {  const [isVisible, setIsVisible] = useState(true);  useEffect(() => {    if (isVisible) {      const timer = setTimeout(() => {        setIsVisible(false);        if (onClose) onClose();      }, duration);      return () => clearTimeout(timer);    }  }, [isVisible, duration, onClose]);  if (!isVisible || !message) return null;  const icon =    type === 'success' ? <CheckCircle size={20} /> :    type === 'error' ? <XCircle size={20} /> :    <Info size={20} />;  return (    <div className={`notification ${type}`}>      {icon}      <span>{message}</span>    </div>  );};export default Notification;