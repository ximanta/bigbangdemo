import React, { useEffect } from 'react'; import { CheckCircle, XCircle } from 'lucide-react'; export const NotificationToast = ({ message, type = 'success', onClose }) => { useEffect(() => { if (message) { const timer = setTimeout(() => { onClose(); }, 3000); return () => clearTimeout(timer); } }, [message, onClose]); if (!message) return null; const Icon = type === 'success' ? CheckCircle : XCircle; return ( <div className="toast-container"> <div className={`toast ${type}`}> <Icon size={20} /> <span>{message}</span> </div> </div> ); };