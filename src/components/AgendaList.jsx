import React from 'react';import { SessionCard } from './SessionCard';export const AgendaList = ({ sessions, onEditSession, onDeleteSession, isOrganizerView }) => {  if (!sessions || sessions.length === 0) {    return <p>No sessions scheduled for this event yet.</p>;  }  const sessionsByDate = sessions.reduce((acc, session) => {    const date = session.date;    if (!acc[date]) {      acc[date] = [];    }    acc[date].push(session);    return acc;  }, {});  const sortedDates = Object.keys(sessionsByDate).sort();  return (    <div className="agenda-list">      {sortedDates.map((date) => (        <div key={date} className="agenda-section">          <h3 className="agenda-day-title">{new Date(date).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' })}</h3>          {sessionsByDate[date].sort((a, b) => a.startTime.localeCompare(b.startTime)).map((session) => (            <SessionCard              key={session.id}              session={session}              onEdit={onEditSession}              onDelete={onDeleteSession}              isOrganizerView={isOrganizerView}            />          ))}        </div>      ))}    </div>  );};