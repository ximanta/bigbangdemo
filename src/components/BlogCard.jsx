import React from 'react';import { Link } from 'react-router-dom';import { Calendar, User } from 'lucide-react';export const BlogCard = ({ post }) => {return (<div className="blog-card card"><img src={post.image} alt={post.title} /><h3><Link to={`/blog/${post.slug}`}>{post.title}</Link></h3><div className="meta"><span><User size={16} /> {post.author}</span><span><Calendar size={16} /> {post.date}</span></div><p>{post.excerpt}</p><Link to={`/blog/${post.slug}`} className="button button-outline">Read More</Link></div>);};