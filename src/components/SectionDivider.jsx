import React from 'react';import { Star } from 'lucide-react';export const SectionDivider = ({ title, description, icon, darkBg = false }) => {const sectionClass = `section-divider section-padding ${darkBg ? 'dark-bg' : ''}`;return (<section className={sectionClass}><div className="container">{icon && <div className="icon">{icon}</div>}<h2>{title}</h2><p>{description}</p></div></section>);};