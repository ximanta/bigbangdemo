import React, { useState } from 'react';import { Quote, Star } from 'lucide-react';export const TestimonialSlider = ({ testimonials }) => {const [currentIndex, setCurrentIndex] = useState(0);const displayedTestimonials = testimonials.slice(currentIndex, currentIndex + 3);const handleNext = () => {setCurrentIndex((prevIndex) =>(prevIndex + 1) % (testimonials.length - 2 > 0 ? testimonials.length - 2 : 1));};const handlePrev = () => {setCurrentIndex((prevIndex) =>(prevIndex - 1 + (testimonials.length - 2 > 0 ? testimonials.length - 2 : 1)) % (testimonials.length - 2 > 0 ? testimonials.length - 2 : 1)));};return (<section className="testimonial-slider section-padding"><div className="container"><h2 className="section-heading">What Our Patients Say</h2><div className="testimonial-slider-wrapper">{displayedTestimonials.map((testimonial) => (<div key={testimonial.id} className="testimonial-card"><Quote className="quote-icon" /><p>{testimonial.quote}</p><div className="rating">{[...Array(testimonial.rating)].map((_, i) => (<Star key={i} fill="#ffc107" strokeWidth={0} />))}</div><p className="author">- {testimonial.name} ({testimonial.procedure})</p></div>))}</div>{testimonials.length > 3 && (<div className="testimonial-slider-controls"><button onClick={handlePrev} aria-label="Previous Testimonial">&#8249;</button><button onClick={handleNext} aria-label="Next Testimonial">&#8250;</button></div>)}</div></section>);};