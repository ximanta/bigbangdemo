import React from 'react';import { Outlet, useLocation } from 'react-router-dom';import Sidebar from './Sidebar';import { User } from 'lucide-react';const EditorLayout = () => {  const location = useLocation();  const getPageTitle = () => {    switch (location.pathname) {      case '/editor/dashboard': return 'Dashboard';      case '/editor/sources': return 'Source Management';      case '/editor/articles': return 'Article Management';      case '/editor/newsletters': return 'Newsletter Creation';      case '/editor/subscribers': return 'Subscriber Management';      case '/editor/analytics': return 'Analytics';      case '/editor/settings': return 'Settings';      default: return 'Editor Panel';    }  };  return (    <div className="flex-container min-h-screen">      <Sidebar />      <div className="flex-col flex-grow">        <header className="main-header">          <h1>{getPageTitle()}</h1>          <div className="flex-container gap-2 items-center">            <User size={20} />            <span>Editor Admin</span>          </div>        </header>        <main className="main-content">          <Outlet />        </main>      </div>    </div>  );};export default EditorLayout;