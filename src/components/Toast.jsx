import React, { useState, useEffect } from 'react';import { Info, CheckCircle, XCircle } from 'lucide-react';function Toast({message, type = 'info', duration = 3000}) {const [isVisible, setIsVisible] = useState(true);useEffect(() => {const timer = setTimeout(() => {setIsVisible(false);}, duration);return () => clearTimeout(timer);}, [duration]);const getIcon = () => {switch (type) {case 'success':return <CheckCircle />;case 'error':return <XCircle />;case 'info':default:return <Info />;}};if (!isVisible) {return null;}return (<div className="toast-container"><div className={`toast ${type}`}><span className="toast-icon">{getIcon()}</span><span>{message}</span></div></div>);}.toast-container {position: fixed;bottom: 80px;left: 50%;transform: translateX(-50%);z-index: 2000;display: flex;flex-direction: column;gap: 10px;}.toast {background-color: var(--text-color);color: var(--surface-color);padding: 12px 20px;border-radius: 8px;box-shadow: 0 4px 12px var(--shadow-color);font-size: 0.9em;display: flex;align-items: center;gap: 10px;}.toast.success {background-color: var(--primary-color);}.toast.error {background-color: #d32f2f;}.toast-icon svg {width: 20px;height: 20px;color: var(--surface-color);}export default Toast;