import React from 'react';import { Link, useLocation } from 'react-router-dom';import { Calendar, PlusCircle, Settings, Users } from 'lucide-react';export const SideMenu = ({ isOpen, onClose }) => {  const location = useLocation();  const handleLinkClick = () => {    onClose();  };  return (    <>      {isOpen && (        <div className="side-menu-overlay open" onClick={onClose}></div>      )}      <nav className={`side-menu ${isOpen ? 'open' : ''}`}>        <div className="side-menu-nav">          <ul>            <li>              <Link                to="/organizer"                onClick={handleLinkClick}                className={location.pathname === '/organizer' ? 'active' : ''}              >                <Calendar size={20} />                My Events              </Link>            </li>            <li>              <Link                to="/organizer/create-event"                onClick={handleLinkClick}                className={location.pathname === '/organizer/create-event' ? 'active' : ''}              >                <PlusCircle size={20} />                Create Event              </Link>            </li>            <li>              <Link                to="/settings"                onClick={handleLinkClick}                className={location.pathname === '/settings' ? 'active' : ''}              >                <Settings size={20} />                Settings              </Link>            </li>          </ul>        </div>      </nav>    </>  );};