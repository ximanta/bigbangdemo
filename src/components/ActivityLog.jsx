const ActivityLog = ({log}) => {return (<div className="activity-log-section"><h3>Activity Log</h3><div className="activity-log-list">{log.length === 0 ? (<p>No activity recorded yet.</p>) : (log.map((activity) => (<div key={activity.id} className="activity-item"><span className="activity-timestamp">{activity.timestamp}</span><span className="activity-description">{activity.description}</span></div>)))}</div></div>);};export default ActivityLog;