import React from 'react';import { Link, useLocation } from 'react-router-dom';import { LayoutDashboard, Rss, Newspaper, Mail, Users, BarChart2, Settings, LogOut } from 'lucide-react';const Sidebar = () => {  const location = useLocation();  const navItems = [    { name: 'Dashboard', icon: LayoutDashboard, path: '/editor/dashboard' },    { name: 'Sources', icon: Rss, path: '/editor/sources' },    { name: 'Articles', icon: Newspaper, path: '/editor/articles' },    { name: 'Newsletters', icon: Mail, path: '/editor/newsletters' },    { name: 'Subscribers', icon: Users, path: '/editor/subscribers' },    { name: 'Analytics', icon: BarChart2, path: '/editor/analytics' },    { name: 'Settings', icon: Settings, path: '/editor/settings' }  ];  return (    <aside className="sidebar">      <div className="sidebar-header">        <h2>NAN Editor</h2>      </div>      <nav className="sidebar-nav">        <ul>          {navItems.map((item) => (            <li key={item.name}>              <Link                to={item.path}                className={location.pathname === item.path ? 'active' : ''}              >                <item.icon size={20} />                {item.name}              </Link>            </li>          ))}        </ul>      </nav>      <div style={{ marginTop: 'auto' }}>        <Link to="/login" className="sidebar-nav-item">          <LogOut size={20} />          Logout        </Link>      </div>    </aside>  );};export default Sidebar;