import {NavLink} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {LayoutDashboard, Ticket, Users, Settings, PlusCircle} from 'lucide-react';const Sidebar = () => {const {currentUser} = useAuth();if (!currentUser) {return null;}const isAdmin = currentUser.role === 'Administrator';const isAgent = currentUser.role === 'Agent';const isCustomer = currentUser.role === 'Customer';return (<aside className="sidebar"><nav className="sidebar-nav"><ul><li><NavLink to="/dashboard"><LayoutDashboard size={20} />Dashboard</NavLink></li>{(isAgent || isAdmin) && (<li><NavLink to="/tickets"><Ticket size={20} />Tickets</NavLink></li>)}<li><NavLink to="/my-tickets"><Ticket size={20} />My Tickets</NavLink></li>{isCustomer && (<li><NavLink to="/new-ticket"><PlusCircle size={20} />New Ticket</NavLink></li>)} {isAdmin && (<li><NavLink to="/users"><Users size={20} />Users</NavLink></li>)}<li><NavLink to="/profile"><Settings size={20} />Profile</NavLink></li></ul></nav></aside>);};export default Sidebar;