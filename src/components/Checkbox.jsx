import React from 'react';const Checkbox = ({ label, id, checked, onChange, disabled = false, className = '' }) => {  return (    <div className="checkbox-group">      <input        type="checkbox"        id={id}        name={id}        checked={checked}        onChange={onChange}        disabled={disabled}        className={className}      />      {label && <label htmlFor={id}>{label}</label>}    </div>  );};export default Checkbox;