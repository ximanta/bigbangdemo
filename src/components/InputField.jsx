import React from 'react';const InputField = ({ label, id, type = 'text', value, onChange, placeholder, required = false, className = '', readOnly = false }) => {  return (    <div className="input-field-group">      {label && <label htmlFor={id}>{label}</label>}      <input        type={type}        id={id}        name={id}        value={value}        onChange={onChange}        placeholder={placeholder}        required={required}        className={className}        readOnly={readOnly}      />    </div>  );};export default InputField;